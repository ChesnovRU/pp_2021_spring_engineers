#ifndef MODULES_TASK_4
#define MODULES_TASK_4

#include <thread>
#include <vector>
#include <string>
using namespace std;

int powervec(vector<int> vect);
vector<int> randomvec(int size);
vector<int> rads(vector<int> vect);
vector<int> rads_thread(vector<int> lisd,int threads);
vector<int> sum(vector<int> vec1, vector<int> vec2);
void f(vector<int>& lisd);

#endif
